## Implementation Tasks

- [x] In `apps/questions/templates/questions/question_form.html`, replace the
  string-values input with a one-value-per-line textarea and update help text.
- [x] In `apps/questions/static/questions/js/question_form.js`, load and save
  string values using newline boundaries.
- [x] In `apps/questions/tests.py`, add `StringVariableFormTests` for markup,
  JavaScript serialization, stored comma-containing values, and generation.
- [x] Run focused tests, JavaScript syntax validation, Django checks, and strict
  OpenSpec validation.
- [x] Archive the completed change.
