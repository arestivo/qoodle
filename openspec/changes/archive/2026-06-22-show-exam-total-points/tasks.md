## Implementation Tasks

- [x] In `apps/exams/views.py`, aggregate pool grades into `total_points` in
  `ExamDetailView`.
- [x] In `apps/exams/templates/exams/exam_detail.html`, display a two-decimal
  total-points badge in the Questions card header.
- [x] In `apps/exams/tests.py`, add `ExamTotalPointsTests` for multiple pools,
  empty exams, templates/versions, and updated grades.
- [x] Run focused tests, full exams-app coverage, Ruff, Django checks, and
  strict OpenSpec validation.
- [x] Archive the completed change.
